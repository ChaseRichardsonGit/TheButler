<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" type="text/css" href="/index.css">
</head>
<body>
  <div class="header">
    <img src="mu.jpeg" alt="My Image" class="image">
    <div class="dropdown">
      <button class="dropbtn">Personas</button>
      <div class="dropdown-content">
        <a href="#">Persona 1</a>
        <a href="#">Persona 2</a>
        <a href="#">Persona 3</a>
      </div>
    </div>
  </div>
  <div class="main">
    <form class="search-form" method="POST" action="/search">
      <input type="text" name="search" placeholder="Search...">
      <button type="submit">Go</button>
    </form>
    <div class="response-pane">
      <% if (results.length > 0) { %>
        <% results.forEach((result) => { %>
          <p><%= result.field %></p>
        <% }); %>
      <% } else { %>
        <p>No results found.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
